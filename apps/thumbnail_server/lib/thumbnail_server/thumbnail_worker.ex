defmodule ThumbnailServer.Worker do
  use Task

  
  def execute({thumbnail_path, store, :min}) do
    
  end

  def execute({thumbnail_path, store, :mid}) do
  end

  def execute({thumbnail_path, store, :max}) do
  end
  
end
